version: "3.9"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: go-sse
    volumes:
      - .:/app
    ports:
      - "8765:8765"
    env_file:
      - .env
    environment:
      - GIN_MODE=debug
